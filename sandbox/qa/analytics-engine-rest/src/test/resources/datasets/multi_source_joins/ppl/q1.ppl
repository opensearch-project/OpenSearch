source=monitor_tracking | where mock_update=false and realm="/mybenefits" and monitor_fail=false | stats count() as total_calls by monitor_id, realm | eval description="Monitor Tracking - MyBenefits Realm" | sort - total_calls