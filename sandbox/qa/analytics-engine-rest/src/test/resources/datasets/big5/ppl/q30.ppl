source = big5 | eval range_bucket = case(`metrics.size` < 0, 'range_1', `metrics.size` >= 0 and `metrics.size` < 100, 'range_2', `metrics.size` >= 100 and `metrics.size` < 1000, 'range_3', `metrics.size` >= 1000, 'range_4') | stats count() by range_bucket
