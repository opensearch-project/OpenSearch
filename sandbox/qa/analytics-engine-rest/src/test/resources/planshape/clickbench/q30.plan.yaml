# 10 constant-shifted SUMs of one column. OpenSearchAggregateConstantShiftRule collapses them
# onto ONE shared SUM + COUNT pair (sum(RW+k) = sum(RW) + k*count(RW)): the shard fragment carries 2
# accumulators over a single CAST(ResolutionWidth) scan column, and the per-term arithmetic runs once in the
# coordinator's final projection. No sort, no group-by, no filter.
query: q30
ppl_file: q30.ppl
applies: [prod2s, prod1s]
plans:
  prod2s:
    post_cbo: |
      OpenSearchProject(sum(ResolutionWidth)=[$0], sum(ResolutionWidth+1)=[ANNOTATED_PROJECT_EXPR(id=3, backends=[datafusion], +($0, $1))], sum(ResolutionWidth+2)=[ANNOTATED_PROJECT_EXPR(id=5, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=4, backends=[datafusion], *(2, $1))))], sum(ResolutionWidth+3)=[ANNOTATED_PROJECT_EXPR(id=7, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=6, backends=[datafusion], *(3, $1))))], sum(ResolutionWidth+4)=[ANNOTATED_PROJECT_EXPR(id=9, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=8, backends=[datafusion], *(4, $1))))], sum(ResolutionWidth+5)=[ANNOTATED_PROJECT_EXPR(id=11, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=10, backends=[datafusion], *(5, $1))))], sum(ResolutionWidth+6)=[ANNOTATED_PROJECT_EXPR(id=13, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=12, backends=[datafusion], *(6, $1))))], sum(ResolutionWidth+7)=[ANNOTATED_PROJECT_EXPR(id=15, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=14, backends=[datafusion], *(7, $1))))], sum(ResolutionWidth+8)=[ANNOTATED_PROJECT_EXPR(id=17, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=16, backends=[datafusion], *(8, $1))))], sum(ResolutionWidth+9)=[ANNOTATED_PROJECT_EXPR(id=19, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=18, backends=[datafusion], *(9, $1))))], viableBackends=[[datafusion]])
        OpenSearchProject($f0=[$0], $f1=[CAST($1):BIGINT NOT NULL], viableBackends=[[datafusion]])
          OpenSearchAggregate(group=[{}], $f0=[SUM($0)], $f1=[SUM($1)], mode=[FINAL], viableBackends=[[datafusion]])
            OpenSearchExchangeReducer(viableBackends=[[datafusion]], exchange=[ExchangeInfo[distributionType=SINGLETON, partitionKeyIndices=[], partitionCount=0]])
              OpenSearchAggregate(group=[{}], agg#0=[SUM($0)], agg#1=[COUNT($0)], mode=[PARTIAL], viableBackends=[[datafusion]])
                OpenSearchProject(ResolutionWidth0=[ANNOTATED_PROJECT_EXPR(id=0, backends=[datafusion], CAST($69):BIGINT)], viableBackends=[[datafusion]])
                  OpenSearchTableScan(table=[[<scrubbed>]], viableBackends=[[lucene, datafusion]])
    fragment: |
      [SHARD_FRAGMENT chosen_backend=datafusion tree_shape=NONE]
      OpenSearchAggregate(group=[{}], agg#0=[SUM($0)], agg#1=[COUNT($0)], mode=[PARTIAL], viableBackends=[[datafusion]])
        OpenSearchProject(ResolutionWidth0=[ANNOTATED_PROJECT_EXPR(id=0, backends=[datafusion], CAST($69):BIGINT)], viableBackends=[[datafusion]])
          OpenSearchTableScan(table=[[<scrubbed>]], viableBackends=[[lucene, datafusion]])
      [COORDINATOR_REDUCE chosen_backend=datafusion tree_shape=NONE]
      OpenSearchProject(sum(ResolutionWidth)=[$0], sum(ResolutionWidth+1)=[ANNOTATED_PROJECT_EXPR(id=3, backends=[datafusion], +($0, $1))], sum(ResolutionWidth+2)=[ANNOTATED_PROJECT_EXPR(id=5, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=4, backends=[datafusion], *(2, $1))))], sum(ResolutionWidth+3)=[ANNOTATED_PROJECT_EXPR(id=7, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=6, backends=[datafusion], *(3, $1))))], sum(ResolutionWidth+4)=[ANNOTATED_PROJECT_EXPR(id=9, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=8, backends=[datafusion], *(4, $1))))], sum(ResolutionWidth+5)=[ANNOTATED_PROJECT_EXPR(id=11, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=10, backends=[datafusion], *(5, $1))))], sum(ResolutionWidth+6)=[ANNOTATED_PROJECT_EXPR(id=13, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=12, backends=[datafusion], *(6, $1))))], sum(ResolutionWidth+7)=[ANNOTATED_PROJECT_EXPR(id=15, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=14, backends=[datafusion], *(7, $1))))], sum(ResolutionWidth+8)=[ANNOTATED_PROJECT_EXPR(id=17, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=16, backends=[datafusion], *(8, $1))))], sum(ResolutionWidth+9)=[ANNOTATED_PROJECT_EXPR(id=19, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=18, backends=[datafusion], *(9, $1))))], viableBackends=[[datafusion]])
        OpenSearchProject($f0=[$0], $f1=[CAST($1):BIGINT NOT NULL], viableBackends=[[datafusion]])
          OpenSearchAggregate(group=[{}], $f0=[SUM($0)], $f1=[SUM($1)], mode=[FINAL], viableBackends=[[datafusion]])
            OpenSearchExchangeReducer(viableBackends=[[datafusion]], exchange=[ExchangeInfo[distributionType=SINGLETON, partitionKeyIndices=[], partitionCount=0]])
              OpenSearchStageInputScan(childStageId=[0], viableBackends=[[datafusion]])
    shard_physical: |
      AggregateExec: mode=Partial, gby=[], aggr=[sum(<scrubbed>.ResolutionWidth) as $f0, count(<scrubbed>.ResolutionWidth) as $f1]
        DataSourceExec: file_groups={<scrubbed>}, projection=[CAST(ResolutionWidth@86 AS Int64) as <scrubbed>.ResolutionWidth], file_type=parquet
    coord_physical: |
      ProjectionExec: expr=[sum(input-0.$f0)@0 as sum(ResolutionWidth), sum(input-0.$f0)@0 + sum(input-0.$f1)@1 as sum(ResolutionWidth+1), sum(input-0.$f0)@0 + 2 * sum(input-0.$f1)@1 as sum(ResolutionWidth+2), sum(input-0.$f0)@0 + 3 * sum(input-0.$f1)@1 as sum(ResolutionWidth+3), sum(input-0.$f0)@0 + 4 * sum(input-0.$f1)@1 as sum(ResolutionWidth+4), sum(input-0.$f0)@0 + 5 * sum(input-0.$f1)@1 as sum(ResolutionWidth+5), sum(input-0.$f0)@0 + 6 * sum(input-0.$f1)@1 as sum(ResolutionWidth+6), sum(input-0.$f0)@0 + 7 * sum(input-0.$f1)@1 as sum(ResolutionWidth+7), sum(input-0.$f0)@0 + 8 * sum(input-0.$f1)@1 as sum(ResolutionWidth+8), sum(input-0.$f0)@0 + 9 * sum(input-0.$f1)@1 as sum(ResolutionWidth+9)]
        AggregateExec: mode=Final, gby=[], aggr=[sum(input-0.$f0), sum(input-0.$f1)]
          CoalescePartitionsExec
            AggregateExec: mode=Partial, gby=[], aggr=[sum(input-0.$f0), sum(input-0.$f1)]
              RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
                StreamingTableExec: partition_sizes=1, projection=[$f0, $f1]
  prod1s:
    post_cbo: |
      OpenSearchProject(sum(ResolutionWidth)=[$0], sum(ResolutionWidth+1)=[ANNOTATED_PROJECT_EXPR(id=3, backends=[datafusion], +($0, $1))], sum(ResolutionWidth+2)=[ANNOTATED_PROJECT_EXPR(id=5, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=4, backends=[datafusion], *(2, $1))))], sum(ResolutionWidth+3)=[ANNOTATED_PROJECT_EXPR(id=7, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=6, backends=[datafusion], *(3, $1))))], sum(ResolutionWidth+4)=[ANNOTATED_PROJECT_EXPR(id=9, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=8, backends=[datafusion], *(4, $1))))], sum(ResolutionWidth+5)=[ANNOTATED_PROJECT_EXPR(id=11, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=10, backends=[datafusion], *(5, $1))))], sum(ResolutionWidth+6)=[ANNOTATED_PROJECT_EXPR(id=13, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=12, backends=[datafusion], *(6, $1))))], sum(ResolutionWidth+7)=[ANNOTATED_PROJECT_EXPR(id=15, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=14, backends=[datafusion], *(7, $1))))], sum(ResolutionWidth+8)=[ANNOTATED_PROJECT_EXPR(id=17, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=16, backends=[datafusion], *(8, $1))))], sum(ResolutionWidth+9)=[ANNOTATED_PROJECT_EXPR(id=19, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=18, backends=[datafusion], *(9, $1))))], viableBackends=[[datafusion]])
        OpenSearchAggregate(group=[{}], agg#0=[SUM($0)], agg#1=[COUNT($0)], mode=[SINGLE], viableBackends=[[datafusion]])
          OpenSearchProject(ResolutionWidth0=[ANNOTATED_PROJECT_EXPR(id=0, backends=[datafusion], CAST($69):BIGINT)], viableBackends=[[datafusion]])
            OpenSearchTableScan(table=[[<scrubbed>]], viableBackends=[[lucene, datafusion]])
    fragment: |
      [SHARD_FRAGMENT chosen_backend=datafusion tree_shape=NONE]
      OpenSearchProject(sum(ResolutionWidth)=[$0], sum(ResolutionWidth+1)=[ANNOTATED_PROJECT_EXPR(id=3, backends=[datafusion], +($0, $1))], sum(ResolutionWidth+2)=[ANNOTATED_PROJECT_EXPR(id=5, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=4, backends=[datafusion], *(2, $1))))], sum(ResolutionWidth+3)=[ANNOTATED_PROJECT_EXPR(id=7, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=6, backends=[datafusion], *(3, $1))))], sum(ResolutionWidth+4)=[ANNOTATED_PROJECT_EXPR(id=9, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=8, backends=[datafusion], *(4, $1))))], sum(ResolutionWidth+5)=[ANNOTATED_PROJECT_EXPR(id=11, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=10, backends=[datafusion], *(5, $1))))], sum(ResolutionWidth+6)=[ANNOTATED_PROJECT_EXPR(id=13, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=12, backends=[datafusion], *(6, $1))))], sum(ResolutionWidth+7)=[ANNOTATED_PROJECT_EXPR(id=15, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=14, backends=[datafusion], *(7, $1))))], sum(ResolutionWidth+8)=[ANNOTATED_PROJECT_EXPR(id=17, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=16, backends=[datafusion], *(8, $1))))], sum(ResolutionWidth+9)=[ANNOTATED_PROJECT_EXPR(id=19, backends=[datafusion], +($0, ANNOTATED_PROJECT_EXPR(id=18, backends=[datafusion], *(9, $1))))], viableBackends=[[datafusion]])
        OpenSearchAggregate(group=[{}], agg#0=[SUM($0)], agg#1=[COUNT($0)], mode=[SINGLE], viableBackends=[[datafusion]])
          OpenSearchProject(ResolutionWidth0=[ANNOTATED_PROJECT_EXPR(id=0, backends=[datafusion], CAST($69):BIGINT)], viableBackends=[[datafusion]])
            OpenSearchTableScan(table=[[<scrubbed>]], viableBackends=[[lucene, datafusion]])
    shard_physical_1seg: |
      ProjectionExec: expr=[sum(<scrubbed>.ResolutionWidth)@0 as sum(ResolutionWidth), sum(<scrubbed>.ResolutionWidth)@0 + count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+1), sum(<scrubbed>.ResolutionWidth)@0 + 2 * count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+2), sum(<scrubbed>.ResolutionWidth)@0 + 3 * count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+3), sum(<scrubbed>.ResolutionWidth)@0 + 4 * count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+4), sum(<scrubbed>.ResolutionWidth)@0 + 5 * count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+5), sum(<scrubbed>.ResolutionWidth)@0 + 6 * count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+6), sum(<scrubbed>.ResolutionWidth)@0 + 7 * count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+7), sum(<scrubbed>.ResolutionWidth)@0 + 8 * count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+8), sum(<scrubbed>.ResolutionWidth)@0 + 9 * count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+9)]
        AggregateExec: mode=Single, gby=[], aggr=[sum(<scrubbed>.ResolutionWidth), count(<scrubbed>.ResolutionWidth)]
          DataSourceExec: file_groups={<scrubbed>}, projection=[CAST(ResolutionWidth@86 AS Int64) as <scrubbed>.ResolutionWidth], file_type=parquet
    shard_physical_nseg: |
      ProjectionExec: expr=[sum(<scrubbed>.ResolutionWidth)@0 as sum(ResolutionWidth), sum(<scrubbed>.ResolutionWidth)@0 + count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+1), sum(<scrubbed>.ResolutionWidth)@0 + 2 * count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+2), sum(<scrubbed>.ResolutionWidth)@0 + 3 * count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+3), sum(<scrubbed>.ResolutionWidth)@0 + 4 * count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+4), sum(<scrubbed>.ResolutionWidth)@0 + 5 * count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+5), sum(<scrubbed>.ResolutionWidth)@0 + 6 * count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+6), sum(<scrubbed>.ResolutionWidth)@0 + 7 * count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+7), sum(<scrubbed>.ResolutionWidth)@0 + 8 * count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+8), sum(<scrubbed>.ResolutionWidth)@0 + 9 * count(<scrubbed>.ResolutionWidth)@1 as sum(ResolutionWidth+9)]
        AggregateExec: mode=Final, gby=[], aggr=[sum(<scrubbed>.ResolutionWidth), count(<scrubbed>.ResolutionWidth)]
          CoalescePartitionsExec
            AggregateExec: mode=Partial, gby=[], aggr=[sum(<scrubbed>.ResolutionWidth), count(<scrubbed>.ResolutionWidth)]
              DataSourceExec: file_groups={<scrubbed>}, projection=[CAST(ResolutionWidth@86 AS Int64) as <scrubbed>.ResolutionWidth], file_type=parquet
