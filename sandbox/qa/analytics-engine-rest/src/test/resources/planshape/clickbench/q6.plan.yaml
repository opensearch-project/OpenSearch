# Generated via -Dplan.generate=write.
query: q6
ppl_file: q6.ppl
applies: [prod1s]
plans:
  prod1s:
    post_cbo: |
      OpenSearchAggregate(group=[{}], dc(SearchPhrase)=[APPROX_COUNT_DISTINCT($0)], mode=[SINGLE], viableBackends=[[datafusion]])
        OpenSearchProject(SearchPhrase=[$74], viableBackends=[[lucene, datafusion]])
          OpenSearchFilter(condition=[ANNOTATED_PREDICATE(id=0, backends=[lucene, datafusion], IS NOT NULL($74))], viableBackends=[[lucene, datafusion]])
            OpenSearchTableScan(table=[[<scrubbed>]], viableBackends=[[lucene, datafusion]])
    fragment: |
      [SHARD_FRAGMENT chosen_backend=datafusion tree_shape=CONJUNCTIVE]
      OpenSearchAggregate(group=[{}], dc(SearchPhrase)=[APPROX_COUNT_DISTINCT($0)], mode=[SINGLE], viableBackends=[[datafusion]])
        OpenSearchProject(SearchPhrase=[$74], viableBackends=[[lucene, datafusion]])
          OpenSearchFilter(condition=[ANNOTATED_PREDICATE(id=0, backends=[lucene, datafusion], IS NOT NULL($74))], viableBackends=[[lucene, datafusion]])
            OpenSearchTableScan(table=[[<scrubbed>]], viableBackends=[[lucene, datafusion]])
    shard_physical_1seg: |
      RelabelExec: schema=Schema { fields: [Field { name: "dc(SearchPhrase)", data_type: Int64, nullable: true }], metadata: {} }
        AggregateExec: mode=Final, gby=[], aggr=[approx_distinct(<scrubbed>.SearchPhrase) as dc(SearchPhrase)]
          CoalescePartitionsExec
            AggregateExec: mode=Partial, gby=[], aggr=[approx_distinct(<scrubbed>.SearchPhrase) as dc(SearchPhrase)]
              RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=1
                QueryShardExec: partitions=1, segments=1, ordering=unsorted
    shard_physical_nseg: |
      RelabelExec: schema=Schema { fields: [Field { name: "dc(SearchPhrase)", data_type: Int64, nullable: true }], metadata: {} }
        AggregateExec: mode=Final, gby=[], aggr=[approx_distinct(<scrubbed>.SearchPhrase) as dc(SearchPhrase)]
          CoalescePartitionsExec
            AggregateExec: mode=Partial, gby=[], aggr=[approx_distinct(<scrubbed>.SearchPhrase) as dc(SearchPhrase)]
              RepartitionExec: partitioning=RoundRobinBatch(4), input_partitions=2
                QueryShardExec: partitions=2, segments=3, ordering=unsorted
