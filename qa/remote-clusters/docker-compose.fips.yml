services:
  opensearch-1:
    environment:
      KEYSTORE_PASSWORD: notarealpasswordphrase
      FIPS_GENERATE_TRUSTSTORE: true
      OPENSEARCH_FIPS_MODE: "true"
  opensearch-2:
    environment:
      KEYSTORE_PASSWORD: notarealpasswordphrase
      FIPS_GENERATE_TRUSTSTORE: true
      OPENSEARCH_FIPS_MODE: "true"
